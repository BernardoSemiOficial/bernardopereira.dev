import { vars } from '@/styles/theme.css'
import { style } from '@vanilla-extract/css'

export const curriculumButton = style({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  paddingBlock: vars.spacing.xxmedium,
})
